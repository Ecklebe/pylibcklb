image: python:3.6-alpine

stages:
  - requirements
  - test
  - install   

installation_of_requirements:
  stage: requirements
  script:
    - pip3 install SIP
    - pip3 install pyqt5
    - pip install -r requirements.txt

run_unit_tests_python_3:
  stage: test
  script:
    - python setup.py test


run_install_of_package:
  stage: install
  script:
    - python setup.py install
